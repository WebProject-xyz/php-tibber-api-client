on:
  push:
    branches:
      - "main"
  pull_request:

name: "CI"

permissions:
  contents: read
  actions: read
  security-events: write

jobs:
  ci:
    uses: "WebProject-xyz/.github/.github/workflows/ci.yml@f205071ae8d915a174281783e1c3f9d8f71421dd" # main
    secrets: inherit
    with:
      php-version: "8.5"
      test-matrix: |
        [
          {"php-version": "8.5", "dependencies": "highest", "with_coverage": true}
        ]
