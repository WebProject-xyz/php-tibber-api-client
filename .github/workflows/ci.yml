on:
  push:
    branches:
      - "main"
  pull_request:

name: "CI"

permissions:
  contents: read
  actions: read
  security-events: write

jobs:
  ci:
    uses: "WebProject-xyz/.github/.github/workflows/ci.yml@f8feb3eb7cba4ff2fe222e02e0dc7706ce22b6b0" # main
    secrets: inherit
    with:
      php-version: "8.5"
      test-matrix: |
        [
          {"php-version": "8.5", "dependencies": "highest", "with_coverage": true}
        ]
